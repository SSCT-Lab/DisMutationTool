package com.example.utils;

/**
 * This class is used for setting the working path of the tool.
 * Please modify ROOT_PATH before running.
 */
public class Config {
    // Root directory for the code.
    // /Users/zhaodongchen/Code/distributedSystems
    // /home/zdc/code/distributedSystems


    public static String ROOT_PATH = "/home/zdc/code";

    // Base directory for the DisMutationTool project.
    public static String PROJECT_PATH = ROOT_PATH + "/DisMutationTool";

    // Directory path where distributed systems are located.
    public static String TARGET_SYSTEM_PATH = ROOT_PATH + "/distributedSystems";

    // Directory for storing mutants generated by the DisMutationTool.
    public static String MUTANT_PATH = PROJECT_PATH + "/mutants";

    // Directory for storing the original code before mutation.
    public static String ORIGINAL_PATH = PROJECT_PATH + "/original";
    public static String ORIGINAL_BYTECODE_PATH = PROJECT_PATH + "/originalBytecode";

    // Directory for storing test resources within the project.
    public static String TEST_RESOURCES_PATH = PROJECT_PATH + "/src/test/resources";

    // Path to the specific project directory of Apache ZooKeeper.
    public static String ZK_PROJECT_PATH = TARGET_SYSTEM_PATH + "/zk/apache-zookeeper-3.5.8/zookeeper-server";

    // Path to the specific project directory of Apache ActiveMQ.(Never used)
    public static String AMQ_PROJECT_PATH = TARGET_SYSTEM_PATH + "/amq/apache-activemq-5.16.0";
    public static String CAS_PROJECT_PATH = TARGET_SYSTEM_PATH + "/cas/apache-cassandra-3.11.6-src";
    public static String HBASE_PROJECT_PATH258 = TARGET_SYSTEM_PATH + "/hbase/hbase-2.5.8/hbase-server";
    public static String HBASE_PROJECT_PATH249 = TARGET_SYSTEM_PATH + "/hbase/hbase-2.4.9/hbase-server";
    public static String RMQ_PROJECT_PATH = TARGET_SYSTEM_PATH + "/rmq/rocketmq-all-5.2.0-source-release/";

    // Path to the script used for running Maven commands.
    public static String SCRIPT_PATH = PROJECT_PATH + "/bin/mvn-runner.sh";
    public static String MVN_SCRIPT_PATH = PROJECT_PATH + "/bin/mvn-runner-no-breaking.sh";
    public static String ANT_SCRIPT_PATH = PROJECT_PATH + "/bin/ant-runner-no-breaking.sh";

    // Directory where test outputs are stored.
    public static String OUTPUTS_PATH = PROJECT_PATH + "/testOutputs";

}
