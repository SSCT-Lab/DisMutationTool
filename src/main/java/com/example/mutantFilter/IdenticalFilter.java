package com.example.mutantFilter;

import com.example.mutator.Mutant;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

import java.util.ArrayList;
import java.util.List;

public class IdenticalFilter {
    Logger logger = LogManager.getLogger(IdenticalFilter.class);

    public static List<Mutant> filter(List<Mutant> mutants) {
        List<Mutant> filteredMutants = new ArrayList<>();

        return filteredMutants;
    }
}
