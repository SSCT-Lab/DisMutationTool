package com.example.utils;

/**
 * This class is used for setting the working path of the tool.
 * Please modify ROOT_PATH before running.
 */
public class Config {
    // Root directory for the code.
    // /Users/zhaodongchen/Code/distributedSystems
    // /home/zdc/code/distributedSystems
    public static final String ROOT_PATH="/home/zdc/code";

    // Base directory for the DisMutationTool project.
    public static final String PROJECT_PATH = ROOT_PATH+"/DisMutationTool";

    // Directory path where distributed systems are located.
    public static final String TARGET_SYSTEM_PATH = ROOT_PATH+"/distributedSystems";

    // Directory for storing mutants generated by the DisMutationTool.
    public static final String MUTANT_PATH = PROJECT_PATH + "/mutants";

    // Directory for storing the original code before mutation.
    public static final String ORIGINAL_PATH = PROJECT_PATH + "/original";
    public static final String ORIGINAL_BYTECODE_PATH = PROJECT_PATH + "/originalBytecode";

    // Directory for storing test resources within the project.
    public static final String TEST_RESOURCES_PATH = PROJECT_PATH + "/src/test/resources";

    // Path to the specific project directory of Apache ZooKeeper.
    public static final String ZK_PROJECT_PATH = TARGET_SYSTEM_PATH+"/zk/apache-zookeeper-3.5.8/zookeeper-server";

    // Path to the specific project directory of Apache ActiveMQ.(Never used)
    public static final String AMQ_PROJECT_PATH = TARGET_SYSTEM_PATH+"/amq/apache-activemq-5.16.0";
    public static final String CAS_PROJECT_PATH = TARGET_SYSTEM_PATH+"/cas/apache-cassandra-3.11.6-src";

    // Path to the script used for running Maven commands.
    public static final String SCRIPT_PATH = PROJECT_PATH + "/bin/mvn-runner.sh";
    public static final String MVN_SCRIPT_PATH = PROJECT_PATH + "/bin/mvn-runner-no-breaking.sh";
    public static final String ANT_SCRIPT_PATH = PROJECT_PATH + "/bin/ant-runner-no-breaking.sh"; // TODO

    // Directory where test outputs are stored.
    public static final String OUTPUTS_PATH = PROJECT_PATH + "/testOutputs";

}
